# HelixMind — ResFinder Worker Dependencies

# Core
resfinder==4.3.3
fastapi==0.111.0
uvicorn[standard]==0.29.0
uvloop==0.19.0           # faster event loop for uvicorn

# Pydantic v2
pydantic==2.7.0

# HTTP client (for internal service-to-service calls)
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
